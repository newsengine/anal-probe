node_modules
*.log

# tenant-isolation local config (contains tenant ids)
examples/tenant-isolation/config.json
examples/security-suite/config.json
# generated scan reports at the repo root (NOT the hosted app's /dashboard/reports route)
/reports/

# agent worktrees (isolation)
.claude/worktrees/
.DS_Store

# local secrets
.dev.vars
**/.dev.vars
**/.env
**/.env.*
